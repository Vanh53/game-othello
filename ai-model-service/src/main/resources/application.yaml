server:
  port: 8082

spring:
  application:
    name: ai-model-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/othello_db?currentSchema=schema_ai}
    username: ${DB_USERNAME:ai_service_acc}
    password: ${DB_PASSWORD:AiPassword@123}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  signerKey: ${JWT_SIGNER_KEY:a5ec3c15e4118e3bf5be7f45a02434dd577a9397a3896e0634b4512148f05aa7}
